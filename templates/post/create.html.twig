{% extends 'base.html.twig' %}

{% block body %}
{# <h1>Créér un post</h1>

{{ form(form) }} #}

<h1>{{ post is defined ? 'Modifier le post' : 'Créer un post' }}</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Enregistrer</button>
{{ form_end(form) }}


{% endblock %}